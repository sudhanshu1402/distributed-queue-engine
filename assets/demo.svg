<svg xmlns="http://www.w3.org/2000/svg" width="1178" height="460" viewBox="0 0 1178 460" role="img" aria-label="jest tests pass with no live Redis: retryable failure, exponential backoff, priority routing and graceful shutdown">
  <rect width="1178" height="460" rx="10" fill="#0d1117" stroke="#30363d"/>
  <path d="M0 10a10 10 0 0 1 10-10h1158a10 10 0 0 1 10 10v28H0z" fill="#161b22"/>
  <circle cx="20" cy="19" r="6" fill="#ff5f57"/><circle cx="38" cy="19" r="6" fill="#febc2e"/><circle cx="56" cy="19" r="6" fill="#28c840"/>
  <text x="74" y="23" font-family="ui-monospace,SFMono-Regular,Menlo,monospace" font-size="12" fill="#7d8590">jest, offline</text>
  <g font-family="ui-monospace,SFMono-Regular,Menlo,monospace" font-size="14" font-weight="500">
    <text x="26" y="54" fill="#3fb950">PASS  email processor boundaries awaits the simulated I/O for the configured duration before deciding</text>
    <text x="26" y="76" fill="#3fb950">PASS  email processor boundaries never fails when the failure rate is 0, even at random() === 0</text>
    <text x="26" y="98" fill="#3fb950">PASS  email processor boundaries surfaces a retryable error so BullMQ applies the configured backoff</text>
    <text x="26" y="120" fill="#3fb950">PASS  email processor boundaries treats random() exactly at the failure rate as a success (strict &lt;)</text>
    <text x="26" y="142" fill="#3fb950">PASS  email processor returns success when the RNG is above the failure threshold</text>
    <text x="26" y="164" fill="#3fb950">PASS  email processor throws a retryable error when the RNG lands in the failure band</text>
    <text x="26" y="186" fill="#3fb950">PASS  email processor waits on the injected sleep for the configured delay</text>
    <text x="26" y="208" fill="#3fb950">PASS  email producer adds a send-notification job carrying the user payload</text>
    <text x="26" y="230" fill="#3fb950">PASS  email producer configures 3 attempts with 5s exponential backoff and removeOnComplete</text>
    <text x="26" y="252" fill="#3fb950">PASS  email producer routes password_reset at high priority (1) and other mail at low priority (10)</text>
    <text x="26" y="274" fill="#3fb950">PASS  graceful shutdown closes the worker then exits with code 0</text>
    <text x="26" y="296" fill="#3fb950">PASS  graceful shutdown exits non-zero when close never settles, rather than hanging until SIGKILL</text>
    <text x="26" y="318" fill="#3fb950">PASS  graceful shutdown exits non-zero when close rejects instead of leaving the promise unhandled</text>
    <text x="26" y="340" fill="#3fb950">PASS  graceful shutdown registers both SIGINT and SIGTERM by default</text>
    <text x="26" y="362" fill="#3fb950">PASS  queue key cluster safety carries a non-empty hash tag, so the key is not hashed whole</text>
    <text x="26" y="384" fill="#3fb950">PASS  queue key cluster safety has only one hash tag, since a second brace pair would not change the slot but signals a naming mistake</text>
    <text x="26" y="406" fill="#3fb950">PASS  queue key cluster safety routes every BullMQ-derived key for this queue to the same hash tag</text>
    <text x="26" y="428" fill="#3fb950">17 passed, 0 failed</text>
  </g>
</svg>
